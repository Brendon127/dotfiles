# /home/brendon/.config/tmuxinator/airvantage.yml

name: collaborator
root: ~/projects/react-native/collaborator-mobile

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim
  - server:
      layout: main-horizontal
      panes:
        - yarn start --reset-cache
        - yarn run android
