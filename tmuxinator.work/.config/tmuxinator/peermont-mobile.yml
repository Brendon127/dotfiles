# /home/brendon/.config/tmuxinator/peermont-mobile.yml

name: peermont-mobile
root: ~/projects/react-native/peermont

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim
  - server:
      layout: main-horizontal
      panes:
        - yarn start --reset-cache
        - yarn run android
