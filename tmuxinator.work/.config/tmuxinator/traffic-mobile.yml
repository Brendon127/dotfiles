# /home/brendon/.config/tmuxinator/peermont-mobile.yml

name: traffic-mobile
root: ~/projects/react-native/traffic-mobile

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim
  - server:
      layout: main-horizontal
      panes:
        - yarn start --reset-cache
        - yarn run android
