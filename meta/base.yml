- defaults:
    link:
      create: true
      relink: true
      force: true
    yum:
      options: "-q -y"
      stdout: False
      stderr: True

- clean:
  - "~"
  - "~/.config"
- shell:
  - [git submodule update --init --recursive, Installing submodules]
