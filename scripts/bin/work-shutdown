#!/usr/bin/env bash

pkill -TERM thunderbird 
pkill -TERM brave 
pkill -TERM spotify
pkill -TERM skype
pkill -TERM zulip
pkill -TERM blueman-applet
pkill -TERM Discord
pkill -TERM GitKraken
pkill -TERM Postman
pkill -TERM pavucontrol
pkill -TERM teams
